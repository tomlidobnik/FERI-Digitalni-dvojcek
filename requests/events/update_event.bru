meta {
  name: update_event
  type: http
  seq: 6
}

put {
  url: https://0.0.0.0:8000/api/event/update
  body: json
  auth: bearer
}

auth:bearer {
  token: 
}

body:json {
  {
    "id": 1,
    "title": "Updated Event",
    "description": "This is an updated test event.",
    "start_date": "2024-06-01T11:00:00",
    "end_date": "2024-06-01T13:00:00",
    "location_fk": 2
  }
}

script:pre-request {
  const token = bru.getVar("token");
  req.setHeader("Authorization", `Bearer ${token}`);
}
