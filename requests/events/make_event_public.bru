meta {
  name: make_event_public
  type: http
  seq: 7
}

put {
  url: https://0.0.0.0:8000/api/event/make_public/2
  body: none
  auth: inherit
}

script:pre-request {
  const token = bru.getVar("token");
  req.setHeader("Authorization", `Bearer ${token}`);
}
