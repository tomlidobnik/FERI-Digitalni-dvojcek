meta {
  name: list_friends
  type: http
  seq: 8
}

get {
  url: https://0.0.0.0:8000/api/user/friends/list
  body: none
  auth: bearer
}

script:pre-request {
  const token = bru.getVar("token");
  req.setHeader("Authorization", `Bearer ${token}`);
}
