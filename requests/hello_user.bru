meta {
  name: hello_user
  type: http
  seq: 2
}

get {
  url: https://0.0.0.0:8000/api/user
  body: none
  auth: bearer
}

auth:bearer {
  token: 
}

script:pre-request {
  const token = bru.getVar("token");
  req.setHeader("Authorization", `Bearer ${token}`);
}
